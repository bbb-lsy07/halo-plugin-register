要解决这个问题，我们需要在插件的后台设置中增加两个自定义字段（比如“品牌 Logo 缩写”和“品牌名称”），然后让前台的 HTML 页面读取这两个配置。

这是一个非常经典的“增加后台配置项并应用到前台”的流程。为了让你清楚整个过程，我将分成 **3个简单的步骤** 来教你修改。

---

### 第一步：在 Java 配置类中增加字段
首先，我们需要在 Java 代码里定义这两个新字段，这样程序才能把设置存入数据库。

打开文件：`src/main/java/run/halo/inviteregister/config/InviteRegistrationSettings.java`

1. 在 `private Boolean enabled = true;` 下方，添加我们需要的两个字段：
```java
    private Boolean enabled = true;

    // --- 新增的代码 开始 ---
    private String brandLogoText = "H";

    private String brandName = "Halo";
    // --- 新增的代码 结束 ---

    private String inviteFieldName = "inviteCode";
```

2. 在文件下方（大约 `getInvalidMessageSafely` 方法下面），添加两个安全获取这些值的方法（防止用户什么都不填导致报错）：
```java
    public String getInvalidMessageSafely() {
        if (invalidMessage == null || invalidMessage.isBlank()) {
            return "邀请码无效、已过期或已被用尽。";
        }
        return invalidMessage.trim();
    }

    // --- 新增的代码 开始 ---
    public String getBrandLogoTextSafely() {
        if (brandLogoText == null || brandLogoText.isBlank()) {
            return "H";
        }
        return brandLogoText.trim();
    }

    public String getBrandNameSafely() {
        if (brandName == null || brandName.isBlank()) {
            return "Halo";
        }
        return brandName.trim();
    }
    // --- 新增的代码 结束 ---
```

---

### 第二步：在设置表单 UI 中添加这两个输入框
Halo 使用 YAML 文件来生成后台的设置表单。你的项目里有两个地方定义了设置，我们需要把输入框加进去。

**修改文件一：** `src/main/resources/settings.yaml`
**修改文件二：** `src/main/resources/extensions/invite-register-settings.yaml`
*(注意：这两个文件的内容是一样的，请把下面的代码加到这两个文件的相同位置)*

找到 `label: 启用邀请码注册` 的那一段，在它的下方加入两段 `$formkit: text` 代码，就像这样：

```yaml
      formSchema:
        - $formkit: checkbox
          name: enabled
          id: enabled
          key: enabled
          label: 启用邀请码注册
          value: true
          
        # --- 新增的代码 开始 ---
        - $formkit: text
          name: brandLogoText
          id: brandLogoText
          key: brandLogoText
          label: 验证页 Logo 文字
          help: 验证页面上方正方形内的单字，默认为 H
          value: H
        - $formkit: text
          name: brandName
          id: brandName
          key: brandName
          label: 验证页品牌名称
          help: 验证页面上方显示的品牌名称，默认为 Halo
          value: Halo
        # --- 新增的代码 结束 ---
        
        - $formkit: text
          name: inviteFieldName
```

---

### 第三步：修改拦截器，把自定义文本填入 HTML
最后一步，我们要修改生成那个验证页面的代码，把写死的 "H" 和 "Halo" 替换成从配置里读取的值。

打开文件：`src/main/java/run/halo/inviteregister/filter/InviteRegistrationFilter.java`

1. 找到 `buildBridgePage` 方法（大约在 310 行左右），我们需要先获取刚才设置的两个值：

```java
    private String buildBridgePage(InviteRegistrationSettings settings, String errorMessage,
        CsrfToken csrfToken) {
        String title = "邀请码注册";
        String description = "请输入有效的邀请码，验证通过后继续完成注册。";
        
        // --- 新增的代码 开始 ---
        String brandLogoText = escapeHtml(settings.getBrandLogoTextSafely());
        String brandName = escapeHtml(settings.getBrandNameSafely());
        // --- 新增的代码 结束 ---
        
        String label = escapeHtml(defaultIfBlank(settings.getInputLabel(), "邀请码"));
```

2. 往下看，在这个方法里的 `return """..."""` 中，找到写死 `H` 和 `Halo` 的 HTML 代码：

将原来的：
```html
                    <div class="invite-brand">
                        <div class="invite-brand__mark">H</div>
                        <span class="invite-brand__text">Halo</span>
                    </div>
```
改成使用占位符 `%s`：
```html
                    <div class="invite-brand">
                        <div class="invite-brand__mark">%s</div>
                        <span class="invite-brand__text">%s</span>
                    </div>
```

3. 继续往下滑，找到 `""".formatted(` 这一部分。因为我们刚刚在 HTML 里增加了两个 `%s`（分别代表Logo字和品牌名），我们需要把 `brandLogoText` 和 `brandName` 传进去。**注意顺序必须和 HTML 里的 `%s` 一一对应**：

原来的代码：
```java
            """.formatted(
            title,
            title,
            description,
```

修改成：
```java
            """.formatted(
            title,          // 对应第一个 %s：网页标签页的 <title>%s</title>
            brandLogoText,  // 对应第二个 %s：<div class="invite-brand__mark">%s</div>
            brandName,      // 对应第三个 %s：<span class="invite-brand__text">%s</span>
            title,          // 对应第四个 %s：<h1>%s</h1>
            description,    // 对应第五个 %s：<p class="invite-desc">%s</p>
```

---

### 第四步：重新编译插件并测试
完成上述修改后，你需要重新打包你的插件：

1. 在终端（Terminal）中运行打包命令：
   ```bash
   gradle clean build
   ```
2. 找到生成的 `.jar` 文件（在 `build/libs/` 目录下）。
3. 进入 Halo 后台，卸载原来的插件，重新安装这个新的 `.jar` 文件。
4. 打开 **Halo Console -> 插件 -> 邀请码注册**，你会发现多出了两个设置项：**验证页 Logo 文字** 和 **验证页品牌名称**。
5. 将它们改成你想要的名称（比如 Logo 填 "博" ，品牌名填 "我的博客"），保存后退出登录/使用无痕模式测试注册页面，就能看到页面上的名字已经变了。

*(提示：关于“跳转后与原风格差别较大”的问题，如果你想修改背景色、字体颜色等，只需要回到刚才的 `InviteRegistrationFilter.java` 中，修改 `<style>...</style>` 里面的 CSS 代码即可，例如修改 `body` 的 `background-color`。)*